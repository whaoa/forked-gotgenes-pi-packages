/**
 * @deprecated This module is superseded by the flat permission config format
 * introduced in #66. It is kept temporarily as a stub while permission-manager.ts
 * is being migrated. It will be deleted in the same step.
 *
 * The universal default is now expressed as permission["*"] in the flat config.
 * mergeDefaults() and getSurfaceDefault() have no replacement — they are
 * eliminated by the flat format.
 */

export {};
